# Claude Notes

Read `AGENTS.md` first. The key project constraint is preserving the ORDS-compatible API during the Oracle ATP to PostgreSQL migration.

Do not remove `/ords/admin/data_main_daily_send/industry` or `/ords/admin/data_main_daily_send/search/` without a frontend migration in the same change.
